(function ($) {
    //    "use strict";

    $('#bootstrap-data-table').DataTable({
		paging: false,
		ordering: false,
		responsive: true,
		autoWidth: false,
    searching: false
    });
})(jQuery);
